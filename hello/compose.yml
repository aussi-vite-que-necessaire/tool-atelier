services:
  app:
    image: ${IMAGE:?IMAGE manquant}
    restart: unless-stopped
    environment:
      APP_ENV: ${APP_ENV:-dev}
    networks:
      lab:
        aliases: ["${UPSTREAM:?UPSTREAM manquant}"]

networks:
  lab:
    name: lab
    external: true
