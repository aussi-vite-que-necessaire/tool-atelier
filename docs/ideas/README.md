# Idées — backlog d'exploration

Pistes capturées pour ne pas les perdre, sans engagement d'implémentation. Chaque fichier suit le gabarit : contexte → l'idée → tradeoffs → quand y revenir.

Capturer une idée : `/noter-idee <sujet>`.

## Idées

- [Endpoint MCP : OAuth par utilisateur + sous-domaine `mcp.contentos.ch`](2026-05-30-mcp-oauth-sous-domaine.md) — À explorer — Passer `/api/mcp` en OAuth par utilisateur et l'exposer sur un sous-domaine dédié.
