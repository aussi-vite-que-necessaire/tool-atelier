# DX locale (base de test isolée + seed dev) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Les tests locaux n'effacent plus la base de dev (`contentos`), et un seul `npm run db:seed -- <email>` repeuple un compte de dev.

**Architecture:** `.env.test` committé (juste `DATABASE_URL=…/contentos_test`) chargé en override par `vitest.config.ts` et `playwright.config.ts`. Script `db:test:prepare` crée + migre `contentos_test`. Seed dev idempotent partagé via `src/lib/db/seeds/`.

**Tech Stack:** Vitest, Playwright, dotenv, Drizzle (node-postgres), pg, tsx.

---

## Task 1 : Helper `adminUrl` (dérive l'URL d'admin pour CREATE DATABASE)

**Files:**
- Create: `src/lib/db/admin-url.ts`
- Test: `test/unit/admin-url.test.ts`

- [ ] **Step 1 : Test qui échoue**

```ts
// test/unit/admin-url.test.ts
import { describe, expect, test } from 'vitest';
import { adminUrl } from '@/lib/db/admin-url';

describe('adminUrl', () => {
  test('remplace le nom de base par "postgres", conserve creds/host/port', () => {
    expect(adminUrl('postgres://app:app@localhost:5432/contentos_test')).toBe(
      'postgres://app:app@localhost:5432/postgres',
    );
  });

  test('dbName extrait la base cible', () => {
    expect(adminUrl('postgres://u:p@h:1/foo')).toBe('postgres://u:p@h:1/postgres');
  });
});
```

- [ ] **Step 2 : Lancer → échoue**

Run: `npm run test:unit -- admin-url`
Expected: FAIL (module introuvable).

- [ ] **Step 3 : Implémentation minimale**

```ts
// src/lib/db/admin-url.ts
// Dérive l'URL de connexion d'admin (base "postgres") à partir d'une URL cible,
// pour pouvoir exécuter CREATE DATABASE avant que la base cible n'existe.
export function adminUrl(targetUrl: string): string {
  const u = new URL(targetUrl);
  u.pathname = '/postgres';
  return u.toString();
}

export function dbNameFromUrl(targetUrl: string): string {
  return new URL(targetUrl).pathname.replace(/^\//, '');
}
```

- [ ] **Step 4 : Lancer → passe**

Run: `npm run test:unit -- admin-url`
Expected: PASS.

- [ ] **Step 5 : Commit**

```bash
git add src/lib/db/admin-url.ts test/unit/admin-url.test.ts
git commit -m "🤖 feat(dx): helper adminUrl pour préparer la base de test"
```

---

## Task 2 : `.env.test` + override dans vitest

**Files:**
- Create: `.env.test`
- Modify: `vitest.config.ts:7`

- [ ] **Step 1 : Créer `.env.test`**

```
# Base dédiée aux tests (vitest + Playwright). Localhost, aucun secret.
# Évite que les tests vident la base de dev (contentos).
DATABASE_URL=postgres://app:app@localhost:5432/contentos_test
```

- [ ] **Step 2 : Charger en override dans `vitest.config.ts`**

Remplacer la ligne `loadEnv();` (ligne 7) par :

```ts
loadEnv();
loadEnv({ path: '.env.test', override: true });
```

- [ ] **Step 3 : Préparer la base de test manuellement (pré-requis du run)**

```bash
docker compose exec -T postgres psql -U app -d postgres -c "CREATE DATABASE contentos_test" 2>/dev/null || true
DATABASE_URL=postgres://app:app@localhost:5432/contentos_test npm run db:migrate
```

Expected: migrations appliquées sur `contentos_test`.

- [ ] **Step 4 : Vérifier l'isolation**

Run: `npm run test:integration -- social-accounts`
Expected: PASS, et la donnée de `contentos` n'est pas touchée (les tests tapent `contentos_test`).

- [ ] **Step 5 : Commit**

```bash
git add .env.test vitest.config.ts
git commit -m "🤖 feat(dx): base de test isolée contentos_test (vitest)"
```

---

## Task 3 : Script `db:test:prepare`

**Files:**
- Create: `scripts/db-test-prepare.ts`
- Modify: `package.json` (scripts)

- [ ] **Step 1 : Écrire le script**

```ts
// scripts/db-test-prepare.ts
#!/usr/bin/env tsx
import { config as loadEnv } from 'dotenv';
import { migrate } from 'drizzle-orm/node-postgres/migrator';
import { drizzle } from 'drizzle-orm/node-postgres';
import { Pool } from 'pg';
import { adminUrl, dbNameFromUrl } from '@/lib/db/admin-url';

loadEnv();
loadEnv({ path: '.env.test', override: true });

async function main(): Promise<void> {
  const target = process.env.DATABASE_URL;
  if (!target) throw new Error('DATABASE_URL manquant');
  const name = dbNameFromUrl(target);

  // 1. Crée la base de test si absente (connexion sur "postgres").
  const admin = new Pool({ connectionString: adminUrl(target) });
  try {
    await admin.query(`CREATE DATABASE ${name}`);
    console.log(`  créé  ${name}`);
  } catch (e) {
    if ((e as { code?: string }).code === '42P04') console.log(`  skip  ${name} (existe déjà)`);
    else throw e;
  } finally {
    await admin.end();
  }

  // 2. Applique les migrations sur la base de test.
  const pool = new Pool({ connectionString: target });
  await migrate(drizzle(pool), { migrationsFolder: './drizzle' });
  await pool.end();
  console.log('  migrations OK');
  process.exit(0);
}

main().catch((e) => {
  console.error(e);
  process.exit(1);
});
```

- [ ] **Step 2 : Ajouter le script npm**

Dans `package.json`, section `scripts`, ajouter :

```json
"db:test:prepare": "tsx scripts/db-test-prepare.ts",
```

- [ ] **Step 3 : Lancer**

Run: `npm run db:test:prepare`
Expected: `skip contentos_test (existe déjà)` (ou `créé`) puis `migrations OK`.

- [ ] **Step 4 : Commit**

```bash
git add scripts/db-test-prepare.ts package.json
git commit -m "🤖 feat(dx): npm run db:test:prepare (crée + migre contentos_test)"
```

---

## Task 4 : Extraire `seedVisualTemplates` (DRY)

**Files:**
- Create: `src/lib/db/seeds/visual-templates.ts`
- Modify: `scripts/seed-visual-templates.ts`

- [ ] **Step 1 : Créer la fonction réutilisable**

```ts
// src/lib/db/seeds/visual-templates.ts
import { createVisualTemplate, getVisualTemplateBySlug } from '../repositories/visual-templates';
import { VISUAL_TEMPLATE_SEEDS } from '@/lib/visual-templates/seeds';

export async function seedVisualTemplates(userId: string): Promise<{ created: number; skipped: number }> {
  let created = 0;
  let skipped = 0;
  for (const seed of VISUAL_TEMPLATE_SEEDS) {
    if (await getVisualTemplateBySlug(userId, seed.slug)) {
      console.log(`  skip  ${seed.slug} (existe déjà)`);
      skipped++;
      continue;
    }
    const row = await createVisualTemplate(userId, seed);
    console.log(row ? `  add   ${seed.slug} → ${row.id}` : `  fail  ${seed.slug} (conflit)`);
    if (row) created++;
  }
  return { created, skipped };
}
```

- [ ] **Step 2 : Réécrire `scripts/seed-visual-templates.ts` pour l'utiliser**

Remplacer la boucle de seed (résolution d'userId conservée) par un appel à `seedVisualTemplates(userId)`, puis log du récap.

- [ ] **Step 3 : Vérifier**

Run: `npx tsx --env-file-if-exists=.env scripts/seed-visual-templates.ts manu.avqn@gmail.com`
Expected: `skip` sur tout (déjà seedé), pas d'erreur.

- [ ] **Step 4 : Commit**

```bash
git add src/lib/db/seeds/visual-templates.ts scripts/seed-visual-templates.ts
git commit -m "🤖 refactor(dx): extrait seedVisualTemplates réutilisable"
```

---

## Task 5 : Données d'exemple + `seedDev` idempotent

**Files:**
- Create: `src/lib/db/seeds/dev-sample.ts`
- Test: `test/integration/seed-dev.test.ts`

- [ ] **Step 1 : Test d'intégration qui échoue**

```ts
// test/integration/seed-dev.test.ts
import { describe, expect, test } from 'vitest';
import { listIdeas } from '@/lib/db/repositories/ideas';
import { listPosts } from '@/lib/db/repositories/posts';
import { getSettings } from '@/lib/db/repositories/settings';
import { seedDev } from '@/lib/db/seeds/dev-sample';
import { createTestUser } from './helpers/seed';

describe('seedDev', () => {
  test('peuple settings + ideas + posts, et reste idempotent', async () => {
    const userId = await createTestUser('seeddev');

    await seedDev(userId);
    const ideas1 = await listIdeas(userId);
    const posts1 = await listPosts(userId);
    expect(await getSettings(userId)).toBeTruthy();
    expect(ideas1.length).toBeGreaterThan(0);
    expect(posts1.length).toBeGreaterThan(0);

    await seedDev(userId); // 2e passe : aucun doublon
    expect((await listIdeas(userId)).length).toBe(ideas1.length);
    expect((await listPosts(userId)).length).toBe(posts1.length);
  });
});
```

- [ ] **Step 2 : Lancer → échoue**

Run: `npm run test:integration -- seed-dev`
Expected: FAIL (module `dev-sample` introuvable).

- [ ] **Step 3 : Implémenter `seedDev`**

```ts
// src/lib/db/seeds/dev-sample.ts
import { createIdea, listIdeas } from '../repositories/ideas';
import { createPost, listPosts } from '../repositories/posts';
import { seedUserDefaults } from './user-defaults';
import { seedVisualTemplates } from './visual-templates';

const SAMPLE_IDEAS = [
  { idea: 'Pourquoi je documente mes specs avant de coder', brief: 'Angle : gain de temps réel vs surcoût perçu.' },
  { idea: 'Le piège des tests qui partagent la base de dev', brief: 'Anecdote : données effacées à chaque run.' },
  { idea: 'Construire en solo avec des agents IA', brief: null },
];

const SAMPLE_POSTS = [
  'Documenter une spec avant de coder, ça paraît lent. En réalité ça évite trois allers-retours.',
  'Mes tests effaçaient ma base de dev à chaque run. Une base de test dédiée a réglé le problème en cinq minutes.',
];

export async function seedDev(userId: string): Promise<void> {
  await seedUserDefaultsIdempotent(userId);
  await seedVisualTemplates(userId);

  const existingIdeas = await listIdeas(userId);
  const byText = new Map(existingIdeas.map((i) => [i.idea, i]));
  for (const s of SAMPLE_IDEAS) {
    if (!byText.has(s.idea)) {
      const created = await createIdea(userId, { idea: s.idea, brief: s.brief ?? undefined });
      byText.set(created.idea, created);
    }
  }

  const ideaIds = SAMPLE_IDEAS.map((s) => byText.get(s.idea)!.id);
  const existingPosts = await listPosts(userId);
  const existingContent = new Set(existingPosts.map((p) => p.content));
  for (let i = 0; i < SAMPLE_POSTS.length; i++) {
    const content = SAMPLE_POSTS[i]!;
    if (!existingContent.has(content)) {
      await createPost(userId, { ideaId: ideaIds[i % ideaIds.length]!, content, status: 'draft' });
    }
  }
}
```

Et rendre `seedUserDefaults` idempotent : créer un wrapper local `seedUserDefaultsIdempotent` qui n'ajoute le template d'écriture que s'il n'existe pas (vérif par slug `linkedin-standard`), les autres (`upsertSettings/Voice/VisualBriefing`) étant déjà des upserts. Implémentation dans `dev-sample.ts` :

```ts
import { listWritingTemplates, createWritingTemplate } from '../repositories/writing-templates';
import { upsertSettings } from '../repositories/settings';
import { upsertVoice } from '../repositories/voice';
import { upsertVisualBriefing } from '../repositories/visual-briefing';
import { DEFAULT_WRITING_TEMPLATE } from './user-defaults';

async function seedUserDefaultsIdempotent(userId: string): Promise<void> {
  await upsertSettings(userId);
  await upsertVoice(userId);
  await upsertVisualBriefing(userId);
  const templates = await listWritingTemplates(userId);
  if (!templates.some((t) => t.slug === DEFAULT_WRITING_TEMPLATE.slug)) {
    await createWritingTemplate(userId, DEFAULT_WRITING_TEMPLATE);
  }
}
```

> Note : vérifier le nom exact de la fonction de liste dans `src/lib/db/repositories/writing-templates.ts` (p.ex. `listWritingTemplates`) et l'export de `DEFAULT_WRITING_TEMPLATE` depuis `user-defaults.ts` ; ajuster les imports si besoin.

- [ ] **Step 4 : Lancer → passe**

Run: `npm run test:integration -- seed-dev`
Expected: PASS.

- [ ] **Step 5 : Commit**

```bash
git add src/lib/db/seeds/dev-sample.ts test/integration/seed-dev.test.ts
git commit -m "🤖 feat(dx): seedDev idempotent (defaults + visuels + ideas + posts)"
```

---

## Task 6 : Script `db:seed` + commande npm

**Files:**
- Create: `scripts/seed-dev.ts`
- Modify: `package.json` (scripts)

- [ ] **Step 1 : Écrire le script orchestrateur**

```ts
// scripts/seed-dev.ts
#!/usr/bin/env tsx
import { eq } from 'drizzle-orm';
import { db } from '@/lib/db/client';
import { user } from '@/lib/db/schema';
import { seedDev } from '@/lib/db/seeds/dev-sample';

async function resolveUserId(arg: string): Promise<string | undefined> {
  if (arg.includes('@')) {
    const rows = await db.select({ id: user.id }).from(user).where(eq(user.email, arg)).limit(1);
    return rows[0]?.id;
  }
  return arg;
}

async function main(): Promise<void> {
  const arg = process.argv[2];
  if (!arg) {
    console.error('Usage: npm run db:seed -- <email|userId>');
    process.exit(1);
  }
  const userId = await resolveUserId(arg);
  if (!userId) {
    console.error(`Aucun user trouvé pour "${arg}". Connecte-toi d'abord, puis relance.`);
    process.exit(1);
  }
  await seedDev(userId);
  console.log(`\nDone. user=${userId}`);
  process.exit(0);
}

main().catch((e) => {
  console.error(e);
  process.exit(1);
});
```

- [ ] **Step 2 : Ajouter la commande npm**

Dans `package.json`, section `scripts`, ajouter :

```json
"db:seed": "tsx --env-file-if-exists=.env scripts/seed-dev.ts",
```

- [ ] **Step 3 : Lancer en réel**

Run: `npm run db:seed -- manu.avqn@gmail.com`
Expected: settings/voice/templates/ideas/posts seedés (ou skip si déjà là), `Done. user=…`.

- [ ] **Step 4 : Commit**

```bash
git add scripts/seed-dev.ts package.json
git commit -m "🤖 feat(dx): npm run db:seed (seed dev en une commande)"
```

---

## Task 7 : Playwright sur contentos_test

**Files:**
- Modify: `playwright.config.ts`

- [ ] **Step 1 : Charger `.env.test` + propager DATABASE_URL**

En tête de `playwright.config.ts` :

```ts
import { config as loadEnv } from 'dotenv';
loadEnv();
loadEnv({ path: '.env.test', override: true });
```

Et dans `webServer.env`, ajouter `DATABASE_URL: process.env.DATABASE_URL!` :

```ts
env: {
  E2E_TESTING: 'true',
  RESEND_API_KEY: '',
  CONTENT_OS_LINKEDIN_STUB: '1',
  DATABASE_URL: process.env.DATABASE_URL!,
},
```

- [ ] **Step 2 : Lancer un E2E**

Run: `CONTENT_OS_LINKEDIN_STUB=1 npm run test:e2e -- test/e2e/linkedin-connection.spec.ts`
Expected: PASS, et la donnée de `contentos` intacte.

- [ ] **Step 3 : Commit**

```bash
git add playwright.config.ts
git commit -m "🤖 feat(dx): Playwright cible contentos_test en local"
```

---

## Task 8 : Documentation `.env.example`

**Files:**
- Modify: `.env.example`

- [ ] **Step 1 : Documenter la base de test + le seed**

Ajouter en commentaire dans `.env.example` (sans dupliquer de secret) :

```
# Tests : base dédiée contentos_test (voir .env.test, committé).
#   npm run db:test:prepare   # crée + migre contentos_test (une fois)
#   npm test                  # n'efface PAS contentos
# Seed d'un compte de dev (après s'être connecté une fois) :
#   npm run db:seed -- <ton-email>
```

- [ ] **Step 2 : Commit**

```bash
git add .env.example
git commit -m "🤖 docs(dx): documente base de test + db:seed dans .env.example"
```

---

## Task 9 : Validation finale + PR

- [ ] **Step 1 : Suite complète**

Run: `npm run db:test:prepare && npm test`
Expected: 256+ tests verts, `contentos` intacte.

- [ ] **Step 2 : Lint + format + tsc**

Run: `npm run format && npm run lint && npx tsc --noEmit`
Expected: clean.

- [ ] **Step 3 : Suite E2E complète**

Run: `CONTENT_OS_AI_STUB=1 CONTENT_OS_PUPPETEER_STUB=1 CONTENT_OS_GEMINI_STUB=1 CONTENT_OS_LINKEDIN_STUB=1 npm run test:e2e`
Expected: tous verts.

- [ ] **Step 4 : Push + PR (ne pas merger)**

```bash
git push -u origin chore/local-dx
gh pr create --base main --title "DX locale : base de test isolée + seed dev" --body "…"
```

- [ ] **Step 5 : Surveiller CI vert**, puis rendre la main à l'utilisateur pour validation du merge.
