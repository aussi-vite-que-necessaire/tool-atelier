export default function Home() {
  return (
    <main className="mx-auto flex min-h-screen max-w-2xl flex-col items-center justify-center gap-6 px-6 text-center">
      <h1 className="text-4xl font-bold tracking-tight text-brand-600 sm:text-5xl">
        auth
      </h1>
      <p className="text-lg text-zinc-500 dark:text-zinc-400">Identity provider central de la suite contentos — SSO partagé entre media, cast, ressources… (BetterAuth, session cross-subdomain).</p>
    </main>
  );
}
