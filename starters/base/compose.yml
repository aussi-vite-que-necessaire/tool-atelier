services:
  app:
    image: ${IMAGE:?IMAGE manquant}
    restart: unless-stopped
    env_file: [.env]
    networks:
      lab:
        aliases: ["${UPSTREAM:?UPSTREAM manquant}"]

networks:
  lab:
    name: lab
    external: true
